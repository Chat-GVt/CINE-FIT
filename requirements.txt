# CINE:FIT 런타임 의존성

# --- API 서버 ---
fastapi
uvicorn[standard]

# --- 자연어 -> MVTI 축 변환 (LLM) ---
anthropic
python-dotenv

# --- BGE-M3 임베딩 (onnxruntime 기반, torch 불필요) ---
onnxruntime
huggingface_hub
tokenizers
numpy
